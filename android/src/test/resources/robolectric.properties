# Default SDK for @RunWith(RobolectricTestRunner::class) tests. It matches
# testOptions.targetSdk in android/build.gradle.kts; the android-all jar for it
# is resolved by Gradle (robolectricAndroidAll), because tests run offline.
sdk=35
