{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://contracts.sellwild.com/bridge-message.schema.json",
  "title": "WebView bridge message",
  "description": "JSON text posted by the SDK-injected script inside the widget WebView (ReactNativeWebView.postMessage, webkit messageHandlers, Android @JavascriptInterface, Flutter JavaScriptChannel). Our scripts produce it.",
  "oneOf": [
    {
      "type": "object",
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "properties": {
        "_synthetic": {
          "description": "Fixture marker. Present only in hand-made fixtures, never on the wire.",
          "const": true
        },
        "type": {
          "const": "LISTING_CLICK",
          "description": "Message type."
        },
        "url": {
          "description": "Listing or product page URL.",
          "type": "string"
        },
        "listing": {
          "description": "Partial listing stub (fields differ per platform).",
          "type": "object"
        }
      }
    },
    {
      "type": "object",
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "properties": {
        "_synthetic": {
          "description": "Fixture marker. Present only in hand-made fixtures, never on the wire.",
          "const": true
        },
        "type": {
          "const": "AD_IMPRESSION",
          "description": "Message type."
        },
        "zoneId": {
          "description": "Zone id (the RN banner omits it).",
          "type": [
            "string",
            "number"
          ]
        }
      }
    },
    {
      "type": "object",
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "properties": {
        "_synthetic": {
          "description": "Fixture marker. Present only in hand-made fixtures, never on the wire.",
          "const": true
        },
        "type": {
          "const": "WIDGET_LOADED",
          "description": "Message type."
        }
      }
    },
    {
      "type": "object",
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "properties": {
        "_synthetic": {
          "description": "Fixture marker. Present only in hand-made fixtures, never on the wire.",
          "const": true
        },
        "type": {
          "const": "ERROR",
          "description": "Message type."
        },
        "message": {
          "description": "In-page error text. Raw: sanitize before logging (bridge.script.exception).",
          "type": "string"
        }
      }
    }
  ]
}
