# Dev-only: stop npm from auto-installing our own peerDependency react-native
# (0.86 wants React 19) next to the React 18 test renderer. Tests stub
# react-native, so it is never needed here. npm never packs .npmrc.
legacy-peer-deps=true
